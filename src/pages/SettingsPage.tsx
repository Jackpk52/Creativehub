import { PageHeader } from '../components/PageHeader'
import { useApp } from '../context/AppContext'

export function SettingsPage() {
  const { data, updateSettings, resetData } = useApp()
  const reset = () => { if (window.confirm('Delete all creations, saved ideas and progress from this browser? This cannot be undone.')) resetData() }
  return <div className="section-wrap page narrow-page"><PageHeader eyebrow="Make it comfortable" title="Settings">Choose what helps you read and create. These preferences stay on this device.</PageHeader><section className="settings-card"><label className="toggle-row"><span><strong>Larger text</strong><small>Increase text size throughout CreativeHub.</small></span><input type="checkbox" checked={data.settings.largeText} onChange={(event) => updateSettings({ largeText: event.target.checked })} /></label><label className="toggle-row"><span><strong>Reduce movement</strong><small>Turn off decorative motion and transitions.</small></span><input type="checkbox" checked={data.settings.reducedMotion} onChange={(event) => updateSettings({ reducedMotion: event.target.checked })} /></label></section><section className="settings-card"><h2>Your data</h2><p>CreativeHub stores creations, progress and settings in this browser only. We do not ask for your name, school, address, location or phone number.</p><button className="button button-danger" onClick={reset}>Delete all my local data</button></section><section className="settings-card"><h2>Need help?</h2><p>If something online makes you worried or uncomfortable, stop and talk with a trusted adult. CreativeHub has no messages, public sharing or contact with strangers.</p><a className="button button-secondary" href="mailto:help@creativehub.example?subject=CreativeHub%20help">Ask for help</a></section></div>
}
